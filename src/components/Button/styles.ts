import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnStyle: {
    width: '100%',
    paddingLeft: 15,
    paddingRight: 15,
    paddingTop: 15,
    paddingBottom: 15,
    fontSize: 15,
    borderRadius: 5
  },
});
