import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnStyle: {
    paddingLeft: 15,
    paddingRight: 15,
    paddingTop: 15,
    paddingBottom: 15,
    fontSize: 15,
    borderRadius: 5,
    lineHeight: 25,
  },
  contStyle: {
    flexDirection: 'row',
  }
});
